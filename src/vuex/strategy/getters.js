/**
 * Created by jun on 2016/11/14.
 */
// 获取专业版宣传页
export const professionalIntro = (state) => {
  return state.strategy.professionalIntro
}

// 获取专业版数据
export const professionalProduct = (state) => {
  return state.strategy.professionalProduct
}

// 获取vip宣传页
export const vipIntro = (state) => {
  return state.strategy.vipIntro
}

// 获取vip数据
export const vipProduct = (state) => {
  return state.strategy.vipProduct
}
